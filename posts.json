---
---

[
  {% for post in site.posts %}
    {
      "url": "{{ post.url | prepend: site.baseurl | prepend: site.url }}",
      "date": "{{post.date}}",
      "title": "{{post.title}}"
    }{% if post.previous %},{% endif %}
  {% endfor %}
]
